{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/scaleway.rdb.v1.DeleteInstanceACLRulesResponse",
  "title": "scaleway.rdb.v1.DeleteInstanceACLRulesResponse",
  "type": "object",
  "properties": {
    "rules": {
      "type": "array",
      "description": "IP addresses defined in the ACL rules of the Database Instance.",
      "items": {
        "$ref": "#/components/schemas/scaleway.rdb.v1.ACLRule"
      }
    }
  },
  "x-properties-order": [
    "rules"
  ]
}