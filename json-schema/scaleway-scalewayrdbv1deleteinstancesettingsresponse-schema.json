{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/scaleway.rdb.v1.DeleteInstanceSettingsResponse",
  "title": "scaleway.rdb.v1.DeleteInstanceSettingsResponse",
  "type": "object",
  "properties": {
    "settings": {
      "type": "array",
      "description": "Settings names to delete from the Database Instance.",
      "items": {
        "$ref": "#/components/schemas/scaleway.rdb.v1.InstanceSetting"
      }
    }
  },
  "x-properties-order": [
    "settings"
  ]
}